# API Configuration
# Để trống = đăng nhập không cần backend (dùng admin / password)
# Có giá trị = gọi API thật (ví dụ: http://localhost:3000)
VITE_API_BASE_URL=

# Enable debug mode
VITE_DEBUG=false
